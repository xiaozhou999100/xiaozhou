选题设计方案：智能制造车间数字孪生监控与运维平台

项目说明

本方案全程只用一台个人电脑（Windows + VSCode）开发，不需要真实车间、不需要物理设备、不需要服务器，所有服务都在本机运行

---

 一、功能需求分析

以智能车间管理人员的工作流程设计 6 个页面，形成：孪生建模 → 数据采集 → 实时监控 → 健康评估 → 预警 → 运维 完整闭环。

1. 车间设备台账
设备 “数字档案”：设备编号、工位、型号、投运日期、孪生模型状态、健康等级；支持增删改查，数据集批量导入。
2. 数字孪生可视化
3D/2D 车间虚拟场景，与物理设备实时同步：设备运行状态、传感器数据、报警点位、产线节拍；支持旋转、缩放、定位查看。
3. 实时数据监测
选择设备，用折线图展示温度、振动、电流、转速等时序数据；异常点自动标红，支持历史回放。
4. 健康与能耗评估
一键评估：设备健康度、能耗效率、负载率；输出趋势曲线与历史评估记录。
5. 孪生预警中心
自动预警：传感器异常、能耗超标、健康度过低、节拍异常；分 提示 / 警告 / 严重 三级，可标记已处理。
6. 运维工单管理
预警一键生成工单，自动带入孪生点位与诊断建议；工单状态：待处理 → 维修中 → 已完成。

核心必做闭环：台账 → 孪生可视化 → 预警 → 工单；数据回放、3D 精细建模可简化。

---

 二、方案论证

 1. 为什么选数字孪生方向

- 智能制造核心方向，课程 / 毕设高度匹配；
- 流程完整：建模 → 同步 → 监控 → 预警 → 运维，技术点全覆盖；
- 纯软件可实现：不需要真实设备，用数据集模拟 “物理端”。

 2. 为什么用公开工业数据集（模拟孪生数据源）

- 免费易获取，国内 AI 平台可下载；
- 包含设备时序数据、工况、故障标签，完美模拟 “孪生数据驱动”；
- 可复现、可演示、答辩好解释。

 3. 为什么用轻量孪生（2D / 伪 3D）不做重型 3D

- 本机可运行，不需要建模软件 / 云端渲染；
- 开发快、演示稳，符合课程设计范围；
- 重点体现**孪生逻辑：虚实同步、数据驱动、映射反馈**。

 4. 为什么用规则引擎 + 轻量模型，不用大模型

- 孪生预警必须稳定、可复现；
- 离线可用，答辩无网也能跑；
- 工业场景要求可追溯：哪条数据超标、哪个点位报警、哪条规则触发。

 5. 为什么用 SQLite

- 数据量适中，完全够用；
- 单文件、免安装、拷贝即运行；
- 最适合本机演示。

---

 三、方案规划

 3.1 技术栈

表格

| 部分 | 选型 | 备注 |
| --- | --- | --- |
| 前端 | Vue3 + Element Plus + ECharts | 快速做后台、画曲线、做面板 |
| 孪生可视化 | 2D 画布 / 轻量组件 | 本机可跑，不依赖 3D 引擎 |
| 后端 | FastAPI (Python 3.10+) | 与算法同语言，接口简单稳定 |
| 数据库 | SQLite | 免服务、便携 |
| 算法 | scikit-learn + 统计计算 | CPU 即可，无 GPU 依赖 |
| 测试 | pytest | 接口与算法测试 |
| 版本管理 | Git + GitHub | 小步提交 |

 3.2 外部资源

- 工业设备时序数据集（机床 / 风机 / 泵类）；
- Python 依赖：fastapi、uvicorn、sqlalchemy、pandas、numpy、scikit-learn、pytest；
- Node.js LTS（前端）；
- 全部在本机安装，无云、无硬件。

 3.3 数据集处理

1. 加列名：设备 ID、时间、工况、传感器；
2. 删除无变化列；
3. 构造健康标签 / 异常标签；
4. 归一化并保存参数；
5. 导入 SQLite，供孪生实时读取。

 3.4 前端 UI（孪生风格）

- 左侧导航 + 右侧孪生大屏；
- 设备卡片：运行 / 待机 / 报警颜色区分；
- 数据面板：实时数值、曲线、健康分；
- 报警点位闪烁高亮。

 3.5 后端与孪生算法

1. **孪生数据同步接口**：读库→推送到前端，实现虚实映射；
2. **异常检测**：孤立森林 / 阈值规则，判断异常；
3. **健康评估**：统计特征 + 评分模型；
4. **规则引擎**：15–25 条规则，触发预警并生成工单。

---

 四、技术路线

```
浏览器（孪生大屏 + 监控页面）
     ↓ HTTP/JSON
FastAPI（孪生同步、评估、预警、工单）
     ↓                ↓
SQLite（设备/数据/工单）   模型/规则文件
```

开发顺序：
数据处理 → 后端接口 → 前端孪生大屏 → 预警与工单 → 联调。



 五、参考文献

[1] 数字孪生与智能制造 [M]. 电子工业出版社.
[2] 工业互联网平台技术 [M]. 机械工业出版社.
[3] 数字孪生体技术白皮书 [EB/OL].
[4] 工业设备数据集公开库 [EB/OL].
